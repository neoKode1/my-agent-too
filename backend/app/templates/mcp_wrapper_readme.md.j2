# {{ project_name }} — MCP Server Wrapper

> Auto-generated by **+12 Monkeys** 🐒

This package wraps [{{ repo_name }}]({{ repo_url }}) as an **MCP (Model Context Protocol) server**, so any LLM or AI agent can call its functionality through standard MCP tool calls.

## What it does

{{ repo_description }}

## Code Mode — 2 Tools, Full Access

This server uses the **Code Mode** pattern (~500 tokens) instead of one-tool-per-operation:

| Tool | Description |
|------|-------------|
| `search` | Explore the repo — list files, read docs, inspect modules. Write Python against a manifest. |
| `execute` | Run Python code against {{ repo_name }} — import modules, call functions, run scripts. |

### Example: discover what's in the repo
```json
{"name": "search", "arguments": {"code": "result = [f for f in manifest['files'] if f.endswith('.py')]"}}
```

### Example: run a function
```json
{"name": "execute", "arguments": {"code": "from {{ repo_name_safe }} import main; result = main.run()"}}
```

## Quick Start

```bash
# 1. Clone this package
git clone <your-package-url>
cd {{ project_name }}

# 2. Set up environment
cp .env.example .env
# Edit .env with your API keys

# 3. Install dependencies
pip install -r requirements.txt

# 4. Run the MCP server
python mcp_server.py
```

The server will clone `{{ repo_url }}` automatically on first run.

## Connect to Claude Desktop

Add this to your `claude_desktop_config.json`:

```json
{
  "mcpServers": {
    "{{ project_name }}": {
      "command": "python",
      "args": ["{{ project_name }}/mcp_server.py"],
      "env": {}
    }
  }
}
```

## Source Repository

- **URL**: {{ repo_url }}
- **Language**: {{ primary_language }}
{% if detected_framework %}- **Framework**: {{ detected_framework }}{% endif %}
{% if entry_points %}- **Entry Points**: {{ entry_points | join(', ') }}{% endif %}

---

Built with [+12 Monkeys](https://github.com/neoKode1/plus12monkeys) 🐒

