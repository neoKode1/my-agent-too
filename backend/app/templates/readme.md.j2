# {{ project_name }}

> {{ template_description }}

**Framework:** {{ framework }}
**Category:** {{ category }}
**Generated by:** [+12 Monkeys](https://github.com/neoKode1/plus12monkeys)

---

## Agents

| Role | Goal |
|------|------|
{% for agent in agents %}
| {{ agent.role }} | {{ agent.goal }} |
{% endfor %}

{% if mcp_servers %}
## MCP Integrations

| Server | Category |
|--------|----------|
{% for server in mcp_servers %}
| {{ server.name }} | {{ server.category }} |
{% endfor %}
{% endif %}

## Quick Start

### Option 1: Docker (recommended)

```bash
cp .env.example .env
# Fill in your API keys in .env
docker compose up --build
```

{% if framework == "vercel-ai" %}
### Option 2: Local Node.js

```bash
cp .env.example .env
# Fill in your API keys in .env
npm install
npx tsx agent.ts
```
{% else %}
### Option 2: Local Python

```bash
cp .env.example .env
# Fill in your API keys in .env
pip install -r requirements.txt
python agent.py
```
{% endif %}

## Environment Variables

| Variable | Required | Description |
|----------|----------|-------------|
| `ANTHROPIC_API_KEY` | ✅ | Your Anthropic API key |
| `LLM_MODEL` | ❌ | LLM model name (default: claude-sonnet-4-20250514) |
{% for server in mcp_servers %}
{% for var in server.required_env %}
| `{{ var }}` | ✅ | Required by {{ server.name }} MCP server |
{% endfor %}
{% endfor %}

## Project Structure

```
{{ project_name }}/
{% if framework == "vercel-ai" %}
├── agent.ts              # Main agent code (Vercel AI SDK)
├── package.json          # Node.js dependencies
├── tsconfig.json         # TypeScript configuration
{% else %}
├── agent.py              # Main agent code ({{ framework }})
├── requirements.txt      # Python dependencies
{% endif %}
├── Dockerfile            # Container build
├── docker-compose.yml    # Container orchestration
├── mcp-config.json       # MCP server configuration
├── .env.example          # Environment variable template
{% if deployment in ("cloud", "export") %}
├── railway.toml          # Railway deployment config
├── render.yaml           # Render deployment config
├── vercel.json           # Vercel deployment config
{% endif %}
└── README.md             # This file
```

## License

Generated for private use. Add your own license as needed.

