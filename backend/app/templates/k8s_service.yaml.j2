# {{ project_name }} — Kubernetes Service
# Generated by +12 Monkeys

apiVersion: v1
kind: Service
metadata:
  name: {{ project_name | replace(' ', '-') | lower }}
  labels:
    app: {{ project_name | replace(' ', '-') | lower }}
    generated-by: plus12monkeys
spec:
  type: ClusterIP
  selector:
    app: {{ project_name | replace(' ', '-') | lower }}
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      name: http

