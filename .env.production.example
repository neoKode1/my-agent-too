# +12 Monkeys — Production Environment Variables
# Copy to .env.production and fill in real values before deploying.

# ─── Required ───────────────────────────────────────────────────
ANTHROPIC_API_KEY=sk-ant-...           # Claude API key (https://console.anthropic.com)

# ─── MongoDB ────────────────────────────────────────────────────
# Railway plugin auto-sets MONGO_URL; for self-hosted use the URI below.
MONGODB_URI=mongodb://mongo:27017
MONGODB_DB=twelve_monkeys

# ─── Backend (FastAPI) ──────────────────────────────────────────
NANDA_URL=http://nanda:6900            # Internal URL of the NANDA registry
CORS_ORIGINS=https://plus12monkeys.vercel.app   # Comma-separated allowed origins
DEBUG=false
LLM_MODEL=claude-sonnet-4-20250514
SESSION_TTL_MINUTES=1440
SESSION_MAX_COUNT=500

# ─── NANDA Index ────────────────────────────────────────────────
PORT=6900
ENABLE_FEDERATION=false

# ─── Frontend (set in Vercel dashboard or via CLI) ──────────────
# NEXT_PUBLIC_API_URL=https://<your-backend>.up.railway.app

