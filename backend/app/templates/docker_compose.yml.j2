# {{ project_name }} — Docker Compose
# Generated by +12 Monkeys

services:
  agent:
    build: .
    container_name: {{ project_name | replace(" ", "-") | lower }}-agent
    env_file: .env
    ports:
      - "8080:8080"
    restart: unless-stopped
{% if mcp_servers %}
    volumes:
      - ./mcp-config.json:/app/mcp-config.json:ro
{% endif %}

